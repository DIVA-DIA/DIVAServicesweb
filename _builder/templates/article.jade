extends layout

block prepend title
  | #{ page.title + ' - '}

block content
  div.container.header
    div.larger
      header.is-small
        h2 #{ page.title }
  div.container.md-content
    div.larger
      aside
        include tutorials-list
      section.md-container!= typogr(page.html).typogrify()